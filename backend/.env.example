# Authorize.Net Configuration
AUTH_NET_API_LOGIN_ID=your_api_login_id
AUTH_NET_TRANSACTION_KEY=your_transaction_key

# Frontend Authorize.Net Configuration (for Accept.js)
VITE_AUTH_NET_CLIENT_KEY=your_client_key
VITE_AUTH_NET_API_LOGIN_ID=your_api_login_id

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (if using)
DATABASE_URL=your_database_url

# Email Configuration (for notifications)
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password

# Application URLs
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3001